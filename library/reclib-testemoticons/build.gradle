apply plugin: 'com.android.library'

android {
    compileSdkVersion 27
    buildToolsVersion '25.0.0'

    defaultConfig {
        minSdkVersion 19
        targetSdkVersion 27
    }

    sourceSets {
        main {
            assets.srcDirs = ['assets']
        }
    }
}